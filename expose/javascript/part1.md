# Part 1 Question Answers

1. 20
2. 20
3. You should not use var because it could cause scoping and naming issues if the same variable is used outside its block.
4. 20
5. The code returned an error because the variable result was not defined outside the block where it was originally defined.
6. The code returns an error because the value of result cannot be reassigned.
7. Returned an error because const variable cannot be reassigned.